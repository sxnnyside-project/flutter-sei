include: package:flutter_lints/flutter.yaml

# DXQE DX-02 — strongest practical correctness guarantees for Dart's
# gradual type system: no implicit dynamic, no unchecked raw types, no
# unsound inference left to chance.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
